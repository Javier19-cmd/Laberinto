console.log("Hola desde utils.")

//Función que devuelve la suma de dos parámetros.
const sum = (a, b) => a + b

//Función que devuelve la resta de dos parámetros.
const sub = (a, b) => a - b

export {sum}
export {sub}